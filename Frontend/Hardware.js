export class Hardware {
    constructor(id, name, tip, info, price) {
        this.id = id;
        this.name = name;
        this.tip = tip;
        this.info = info;
        this.price = price;
    }
}